import { CourseColorDirective } from './course-color.directive';

describe('CourseColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseColorDirective();
    expect(directive).toBeTruthy();
  });
});
