import { IconsTextPipe } from './icons-text.pipe';

describe('IconsTextPipe', () => {
  it('create an instance', () => {
    const pipe = new IconsTextPipe();
    expect(pipe).toBeTruthy();
  });
});
